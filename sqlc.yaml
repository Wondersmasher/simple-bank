version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/query/"
    schema: "./db/schema/"
    gen:
      go:
        package: "simple_bank"
        out: "simple_bank"
        sql_package: "pgx/v5"
        emit_json_tags: true
